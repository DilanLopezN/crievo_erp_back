// ============================================================
// BASE: Configuração do Prisma
// Generator e Datasource compartilhados por todos os schemas
// ============================================================

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["core", "rh", "financeiro"]
}
